package com;

public interface Metodos {
	/*
	 * Las expresiones lambda necesitan de una
	 * interface funcional para ser utilizadas.
	 * 
	 * Una interface funcional es aquella que 
	 * tiene un método abstracto.
	 * 
	 * Métodos abstractos no tienen cuerpo.
	 */
	 
	//public void mensaje(String nombre);
	public void operacion(int a, int b);
}
